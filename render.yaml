services:
  - type: web
    name: teacher-training-assessment
    env: node
    nodeVersion: 18
    plan: standard
    buildCommand: pnpm install --frozen-lockfile && pnpm db:push && NODE_OPTIONS="--max-old-space-size=4096" pnpm build
    startCommand: NODE_ENV=production node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_OPTIONS
        value: --max-old-space-size=4096